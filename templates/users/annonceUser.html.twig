{% extends 'base.html.twig' %}

{% block title %}Mes annonces{% endblock %}
{% block body %}
<div class="container">
    <h1>Mes annonces: </h1>
    {% for annonces in annonce %}
        <li class="ann">
<div class = "row">
    <div class="col-sm-10">
                    <p>Titre : {{ annonces.title }}</p>
                    <p>Description : {{ annonces.description }}</p>
                    <p>Prix : {{ annonces.price }}</p>
    </div>
    <div class="col-sm-2">
            <a class="btn btn-primary" href="{{ path('ad_edit', {'id': annonces.id}) }}"><i class="fas fa-edit"></i></a>
            {{ include('users/_delete_form_annonce.html.twig') }}
            </div>
</div>

        </li>

    {% endfor %}

</div>
{% endblock %}