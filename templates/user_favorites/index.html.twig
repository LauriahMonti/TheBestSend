{% extends 'base.html.twig' %}

{% block title %}UserFavorites index{% endblock %}

{% block body %}
    <h1>UserFavorites index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Created</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user_favorite in user_favorites %}
            <tr>
                <td>{{ user_favorite.id }}</td>
                <td>{{ user_favorite.created ? user_favorite.created|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('user_favorites_show', {'id': user_favorite.id}) }}">show</a>
                    <a href="{{ path('user_favorites_edit', {'id': user_favorite.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('user_favorites_new') }}">Create new</a>
{% endblock %}
